# GoChat

A real-time CLI chat app built in Go, using an RPC server and SQLite to keep messages saved. It includes a simple CLI with login + signup, and the ability to fetch + send messages.

## Tech Stack
- GoLang (Go)
- SQLite
- net/rpc
